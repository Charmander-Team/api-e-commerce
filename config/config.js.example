/**
 * You must have to:
 *  - copy this files and rename it "config.js"
 *  - edit this one with your sql ID and choose a port to launch the API
 */

const sql_config = {
    host: 'localhost',
    user: 'root',
    password: 'root',
    database: 'eshop_pkm',
}

const api_port = 3000;

module.exports.sql_config = sql_config;
module.exports.api_port = api_port;



